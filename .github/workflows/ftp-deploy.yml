name: Deploy React + PHP Puro (Build Simples e FTP)

on:
  push:
    branches:
      - main # O deploy ocorre após um push para a branch 'main'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout do Código
        uses: actions/checkout@v4

      # --- 1. BUILD DO FRONTEND (REACT) ---
      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' 

      - name: 📦 Instalar Dependências do React
        run: npm install
        # 🎯 Usa a pasta correta: ./frontend
        working-directory: ./frontend 

      - name: 🔨 Build do Projeto React
        run: npm run build
        # O output estático será gerado em: ./frontend/build/
        working-directory: ./frontend 
        
  # ... passos 1. BUILD DO FRONTEND (REACT) ...
        
# --- 2. DEPLOY FTP (TUDO PARA A RAIZ) ---
      
      # A) Deploy dos Ficheiros Estáticos (React Build)
      - name: ⏫ A) Deploy do Frontend (React)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: './frontend/build/' 
          remote-dir: '/' 
          file-permissions: 0644   # <-- Adicionar esta linha
          folder-permissions: 0755 # <-- Adicionar esta linha

      # B) Deploy dos Ficheiros de Backend (PHP Puro)
      - name: ⏫ B) Deploy dos Ficheiros PHP Puro
        uses: SamKirkland/FTP-Deploy-Action@v4.3.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: './' 
          remote-dir: '/'
          exclude: |
            **/node_modules/**
            .git/
            .github/
            /frontend
            /backend
          file-permissions: 0644   # <-- Adicionar esta linha
          folder-permissions: 0755 # <-- Adicionar esta linha

            