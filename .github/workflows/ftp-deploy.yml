name: Deploy React + PHP Puro (Build e FTP)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout do Código
        uses: actions/checkout@v4

      # --- Build do Front-end (React) ---
      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Instalar Dependências do Front-end (React)
        run: npm install
        # 🎯 CORREÇÃO: Define o diretório de trabalho correto
        working-directory: ./easypass-frontend/frontend 

      - name: 🔨 Build do Projeto React (Gera a pasta 'build')
        run: npm run build
        # 🎯 CORREÇÃO: Define o diretório de trabalho correto
        working-directory: ./easypass-frontend/frontend 

      # --- Deploy via FTP ---
      - name: ⏫ FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # 💡 ATENÇÃO: É PRECISO ESPECIFICAR A PASTA DE ORIGEM DO DEPLOY!
          # A pasta de build/produção (que é o output do npm run build) 
          # está agora em ./easypass-frontend/frontend/build/
          # Se o seu servidor espera apenas o conteúdo da pasta 'build' na raiz:
          local-dir: './frontend/build/' 
          remote-dir: '/' # Raiz do subdomínio (public_html)